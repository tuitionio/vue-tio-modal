/**
 * Sets up the the notification action states
 *
 * @export
 * @enum {string}
 */
enum NotificationActions {
  MODAL_CLOSED = 'MODAL_CLOSED',
  MODAL_OPENED = 'MODAL_OPENED',
}
export default NotificationActions;